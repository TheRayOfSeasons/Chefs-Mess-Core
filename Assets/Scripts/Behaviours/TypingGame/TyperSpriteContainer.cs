using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/**
    Use this script to handle custom sprite implementations
    for each letter in the typing game.
 */
public class TyperSpriteContainer : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
